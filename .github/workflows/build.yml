name: Build Actions Playground
on: [push]
jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Check location
        run: pwd; ls -la
      - name: Run build script
        run: bash build.sh
      - name: List Binary
        run: ls -la target/release/actions-playground
